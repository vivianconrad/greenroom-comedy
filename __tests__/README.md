# Tests

Run tests with `npm test`.

Current coverage:

- `utils.test.js` — `cn`, `formatDate`, `daysUntil`, `slugify` from `lib/utils.js`

Add new test files alongside the code they test or in this directory.
